import 'package:flutter/material.dart';

class AppColors {
  static const Color primaryColor = Color.fromRGBO(0, 99, 13, 1.0);

  // Tambahkan warna lain di sini jika diperlukan
  static const Color secondaryColor = Color.fromRGBO(255, 87, 34, 1.0); // Contoh warna lain
  static const Color backgroundColor = Color.fromRGBO(240, 240, 240, 1); // Contoh warna latar belakang
}
